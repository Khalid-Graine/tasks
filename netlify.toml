[build]
  # Ensure dependencies are installed inside the subfolder before building
  command = "cd my-task-app && npm install && npm run build"
  publish = "my-task-app/dist"

[dev]
  command = "cd my-task-app && npm run dev"
  port = 5173
